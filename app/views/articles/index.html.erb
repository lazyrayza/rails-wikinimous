<h1>BEST ARTICLES IN THE WROLD</h1>
<div class="bigbox">

<% @articles.each do |article| %>

  <div class="card" class="view overlay zoom" style="width: 18rem;">
    <img src="https://source.unsplash.com/random?sig=<%= (1..10000).to_a.sample %>" class="img-fluid " alt="smaple image">
    <div class="mask flex-center">
   <p class="white-text"><%= link_to article.title , article_path(article)%> </p>
 </div>
   <button style="background: #DDF4FF"> <%= link_to "Delete", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %></button>
  </div>
<% end %>

</div>
<%= link_to 'Add new article to the BEST ARTCILES IN THE WORLD list', articles_new_path %>

